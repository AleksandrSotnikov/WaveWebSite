# Wave Studio - Калькулятор Абонементов и Отчётов

Профессиональная система управления абонементами, расчёта доходов тренеров и формирования отчётов для танцевальной студии Wave.

## 📋 Описание

Приложение предназначено для:
- Управления клиентами и их абонементами (лимитированные и безлимитные)
- Регистрации тренировок и отслеживания посещаемости
- Автоматического расчёта прибыли тренеров на основе формулы комиссии (45%)
- Генерации отчётов в форматах CSV, PDF, HTML
- Планирования занятий с визуализацией статуса абонементов

## 🏗️ Архитектура проекта

```
WaveWebSite/
├── backend/                 # Node.js + Express API
│   ├── src/
│   │   ├── models/         # Моделе данных (Client, Trainer, Subscription, Session)
│   │   ├── routes/         # API routes
│   │   ├── controllers/    # Бизнес-логика
│   │   ├── middleware/     # Аутентификация, валидация
│   │   ├── services/       # Расчёты, отчёты
│   │   └── utils/          # Вспомогательные функции
│   ├── migrations/         # Database migrations (Sequelize/TypeORM)
│   ├── tests/              # Unit и integration tests
│   ├── .env.example        # Переменные окружения
│   └── package.json
├── frontend/               # React приложение
│   ├── src/
│   │   ├── components/     # Компоненты (Clients, Trainers, Sessions, Reports)
│   │   ├── pages/          # Страницы (Dashboard, Admin, Scheduler)
│   │   ├── services/       # API клиент
│   │   ├── store/          # State management (Redux/Context)
│   │   ├── styles/         # Глобальные стили
│   │   └── App.jsx
│   ├── package.json
│   └── .env.example
├── database/               # Schema и seed data
│   └── schema.sql
├── docs/                   # Документация
│   ├── API.md              # OpenAPI спецификация
│   ├── DATA_MODELS.md      # Подробное описание моделей
│   └── SETUP.md            # Инструкция по установке
└── docker-compose.yml      # Контейнеризация (PostgreSQL, Node, React)
```

## 🔧 Стек технологий

**Backend:**
- Node.js 18+
- Express.js
- PostgreSQL
- Sequelize ORM
- JWT для аутентификации
- bcryptjs для хеширования паролей

**Frontend:**
- React 18
- Vite для быстрой сборки
- Axios для HTTP запросов
- React Router для навигации
- TailwindCSS для стилизации
- Chart.js для визуализации доходов

**Отчёты:**
- PapaParse (CSV)
- PDFKit (PDF)
- HTML таблицы

## 📊 Модели данных

### Client
```json
{
  "id": "integer",
  "full_name": "string",
  "phone_number": "string +7XXXXXXXXXX",
  "messenger_link": "string (VK/Telegram URL)",
  "active_subscriptions": ["integer[]"]
}
```

### Trainer
```json
{
  "id": "integer",
  "full_name": "string"
}
```

### Subscription
```json
{
  "id": "integer",
  "type": "limited | unlimited",
  "price": "number",
  "total_sessions": "integer (null для unlimited)",
  "start_date": "YYYY-MM-DD",
  "expiration_date": "YYYY-MM-DD",
  "status": "active | expired"
}
```

### Session
```json
{
  "id": "integer",
  "trainer_id": "integer",
  "date_time": "ISO 8601",
  "clients": ["integer[]"],
  "subscription_used": ["integer[]"],
  "timezone": "string (UTC+6)"
}
```

## 🚀 Быстрый старт

### Требования
- Node.js 18+
- PostgreSQL 14+
- Docker & Docker Compose (опционально)

### Установка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/AleksandrSotnikov/WaveWebSite.git
cd WaveWebSite
```

2. Установите зависимости:
```bash
cd backend && npm install
cd ../frontend && npm install
```

3. Настройте переменные окружения:
```bash
cp backend/.env.example backend/.env
cp frontend/.env.example frontend/.env
```

4. Инициализируйте БД:
```bash
cd backend
npm run migrate
npm run seed
```

5. Запустите приложение:
```bash
# Terminal 1: Backend
cd backend && npm start

# Terminal 2: Frontend
cd frontend && npm run dev
```

Приложение будет доступно по адресу: `http://localhost:3000`

## 📖 Документация

- [API Reference](./docs/API.md)
- [Модели данных](./docs/DATA_MODELS.md)
- [Инструкция по установке](./docs/SETUP.md)

## 💰 Формулы расчёта

### Лимитированный абонемент (n занятий по цене x)
```
Прибыль тренера = (x * 0.45) / n × количество_занятий_тренера
```

### Безлимитный абонемент (цена x)
```
Прибыль тренера = (x * 0.45) / общее_количество_занятий × количество_занятий_тренера
```

## ✅ Основные функции

- ✅ Управление клиентами (добавление, редактирование, удаление)
- ✅ Управление тренерами и их расписанием
- ✅ Создание и управление абонементами
- ✅ Регистрация тренировок с автоматическим расчётом доходов
- ✅ Планировщик с фильтрацией по месяцам и статусу абонементов
- ✅ Генерация отчётов (CSV, PDF, HTML)
- ✅ Система аутентификации для администраторов
- ✅ Валидация конфликтов тренировок
- ✅ Визуализация активных/просроченных абонементов

## 🔒 Безопасность

- Пароли хранятся в виде bcrypt хешей
- JWT токены для аутентификации
- CORS защита
- SQL injection prevention (ORM)
- Input validation на frontend и backend

## 📝 Лицензия

MIT

## 👨‍💼 Контакты

Разработано для Wave Studio

---

**Статус проекта:** 🔨 В разработке (фаза инициализации)
