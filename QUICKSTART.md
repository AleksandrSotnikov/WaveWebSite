# 🚀 НБИГАть с Wave Studio

## Установка в 10 минут 💫

### Предварительные требования
- ✅ Docker и Docker Compose установлены
- ✅ Git установлен
- ✅ ~1GB свободного места на диске

---

## Шаг 1: Клонирование

```bash
git clone https://github.com/AleksandrSotnikov/WaveWebSite.git
cd WaveWebSite
```

---

## Шаг 2: Создание .env файлов

### Backend данные
```bash
cp backend/.env.example backend/.env
```

Текущие валюы в backend/.env готовы, ни жда изменения окружения.

### Frontend данные
```bash
cp frontend/.env.example frontend/.env
```

---

## Шаг 3: Открытие Docker контейнеров

```bash
docker-compose up -d
```

Ожидайте 3-5 секунд для достаточного PostgreSQL готовится.

---

## Шаг 4: Проверка установки

```bash
# Проверить health check Backend
curl http://localhost:5000/health

# Результат должен выглядеть так:
# {"status":"OK","timestamp":"2025-12-16T10:40:00.000Z","uptime":5.123}
```

---

## Шаг 5: Открытие пприложения

Открыте ваш браузер и навигируйте на:

```
http://localhost:3000
```

---

## Авторизация

**Учетные данные:**
```
username: admin
password: Admin123456
```

🔍 **ИМПОРТАНТО:** Помните, что если это продукция, ОБЕЗАТЕЛЬНО от открывать дефолт точки доступа.

---

## Контроль текущих показателей

### Показывать мониторинг
```bash
# Все логи
docker-compose logs -f

# Только Backend
docker-compose logs -f wave_backend

# Только Frontend
docker-compose logs -f wave_frontend

# Только PostgreSQL
docker-compose logs -f wave_postgres
```

### Остановить
```bash
docker-compose down
```

### Перестартовать
```bash
docker-compose restart
```

### Очистить данные и все волюмена (ОПАСНО!)
```bash
docker-compose down -v
```

---

## URL-адреса для Определения

| Сервис | URL | Этап | Установка |
|---------|-----|--------|--------------|
| Frontend | http://localhost:3000 | ✅ | ✅ |
| Backend API | http://localhost:5000/api | ⏳ | ✅ |
| Health Check | http://localhost:5000/health | ✅ | ✅ |
| PostgreSQL | localhost:5432 | ✅ | ✅ |

---

## Троблешутинг

### " Другая сохранная этот порт"

```bash
# Linux/Mac
lsof -i :3000
lsof -i :5000
lsof -i :5432

# Windows
netstat -ano | findstr :3000
netstat -ano | findstr :5000
netstat -ano | findstr :5432

# Остановить процесс
kill -9 <PID>
```

### Контейнер не запускается

```bash
# Остановить всё и удалить волюмена
docker-compose down -v

# Пересобрать образы
docker-compose build --no-cache

# Открыть снова
docker-compose up -d
```

### PostgreSQL не готов (время старта)

```bash
# Ожидать 10 секунд и потом неаорите прыга
# Проверить банка данных
sleep 5
docker-compose exec wave_postgres psql -U wave_user -d wave_studio -c "SELECT 1;"
```

---

## Структура проекта

```
WaveWebSite/
├── backend/              Node.js + Express API (готовое около)
├── frontend/             React + Vite (boilerplate)
├── database/             PostgreSQL schema
├── docs/                 Полная документация
├── docker-compose.yml    Оркестрация сервисов
├── README.md             Полная инструкция
├── QUICKSTART.md         Срочные ответы (этот файл)
├── PROJECT_SUMMARY.md    Отчёт с фтатуссом
└── DEVELOPMENT_CHECKLIST.md  План разработки
```

---

## На следующие работы

ОПределите [DEVELOPMENT_CHECKLIST.md](./DEVELOPMENT_CHECKLIST.md) в жодать Сописэ всех фаз разработки:

- **Фаза 1**: Backend API (ПОие работы)
- **Фаза 2**: Frontend UI
- **Фаза 3**: Отчёты в виде PDF
- **Фаза 4**: Тестирование
- **Фаза 5**: Deployment
- **Фаза 6**: Отличение

---

## Полезные ресурсы

- **Backend документация**: `/docs/API.md`
- **Модели данных**: `/docs/DATA_MODELS.md`
- **Инструкция по установке**: `/docs/SETUP.md`
- **Генератор данных**: `/database/schema.sql`

---

## Успеха 🚀

Несли всё работает правильно, вы выдите:

1. 💜 Frontend достаточна используемая и реакизованых на http://localhost:3000
2. 📄 Backend API выпускирует ответы хедди он http://localhost:5000/api
3. 💾 PostgreSQL готов и готов для работы

🌟 Можно быстро ПФА раработки на ОИтвод!
